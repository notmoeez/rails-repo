<h1>Portfolio Items</h1>
<p><%= notice%></p>
<%= link_to "Create new item", new_portfolio_path if logged_in?(:site_admin)%>
<%= render partial: 'portfolio_item', collection: @portfolio_items, spacer_template: 'portfolio_item_ruler' %>